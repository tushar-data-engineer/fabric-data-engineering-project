# fabric-data-engineering-project
Data engineering pipeline built using Microsoft Fabric that extracts data from a public API and stores it in a Lakehouse using a Medallion Architecture (Bronze, Silver, Gold). The pipeline definition is version-controlled in Git for backup and collaboration.
